# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# CORS
# Comma-separated list of allowed origins for browser requests
# For local dev with CRA frontend on 3000:
ALLOWED_ORIGIN=http://localhost:3000

# Firebase Admin
# Option A: provide inline service account JSON (preferred in CI as a secret)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}
# Option B: reference a credential file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Base project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# QR / Crypto
# Required to generate and validate QR tokens
HMAC_SECRET=replace-with-a-strong-random-secret

# Feature flags
FEATURE_PAYMENTS_ENABLED=false

# Stripe (only when FEATURE_PAYMENTS_ENABLED=true)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
