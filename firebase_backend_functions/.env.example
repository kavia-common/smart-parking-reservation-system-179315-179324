# Environment configuration for firebase_backend_functions

# Base server settings
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# CORS configuration
# Comma-separated list of allowed origins, e.g. http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGIN=http://localhost:3000

# Security/HMAC for webhook signing or internal tokens
HMAC_SECRET=change_this_to_a_long_random_secret_value

# Feature flags
# Toggle payments feature; if true, STRIPE_SECRET_KEY is required for payment flows
FEATURE_PAYMENTS_ENABLED=false

# Stripe secret key (only required if FEATURE_PAYMENTS_ENABLED=true)
# Obtain from Stripe dashboard: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=

# Firebase Admin SDK
# Option A: Use ADC (Application Default Credentials) path to service account file within the container
# You can set GOOGLE_APPLICATION_CREDENTIALS to a mounted path of service account JSON when running in CI/CD
GOOGLE_APPLICATION_CREDENTIALS=

# Option B: Inline service account JSON (preferred for containerized CI if secret management injects JSON string)
# Paste the entire JSON as a single line or escaped string injected via CI secret management
FIREBASE_SERVICE_ACCOUNT_JSON=

# Firebase project ID (required)
FIREBASE_PROJECT_ID=your-firebase-project-id
